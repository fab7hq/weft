[package]
name = "weftd"
version = "0.0.1"
edition = "2024"

# The daemon: pane processes, the record on disk, and the RingFrame CLI. One
# per machine, holding every project anyone opens (ADR-0007).
[dependencies]
anyhow = "1.0.104"
portable-pty = "0.9.0"
serde_json = "1.0.151"
vt100 = "0.16.2"
weft-core = { path = "../weft-core" }
weft-proto = { path = "../weft-proto" }
